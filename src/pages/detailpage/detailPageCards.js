import React, { useEffect } from "react";
import { Axios } from "axios";

export default function DetailPageCard() {

// const courseId = props.match.params.courseId

    
    useEffect(() => {
        // Axios.get(`/api/course/course_by_id?id=${courseId}&type=single`)
           
    },[])
    
    return (
        <div>

        </div>
    )
}